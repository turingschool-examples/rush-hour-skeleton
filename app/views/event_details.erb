<%= partial 'partials/navigation' %>

<h2>Application Event Details</h2>

	<h4>How many times the event was received overall:</h4>
	CODE HERE


	<h4>Hour by Hour Breakdown of when the event was received:</h4>

	<ol id>
		<%@events.each do |(event, count)| %>
		<li> <a href="/sources/<%= @source.identifier%>/events/<%= event.name %>"><%=event.name%></a></li>
		<% end %>

	</ol>

</div>
