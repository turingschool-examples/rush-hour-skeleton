class EventDetails < ActiveRecord::Base

	def capture_the_hour

	end
	

	def hour_by_hour_breakdown
		#take in parsed requestedAt time
		#call .hour on it to find the correct hour
		#display the hours in a list
	end
	
end