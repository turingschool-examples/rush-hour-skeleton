class PayloadParser

  # def parser(payload)
  #   #before or after all of this, we would take and put it in its respective object using find_or_create_by
  #   url = Url.find_or_create_by(url: params["url"]),
  #   requested_at: params["requested_at"],
  #   referred_by = ReferredBy.find_or_create_by(referred_by_id: params["referred_by_id"]),
  #   request_type = RequestType.find_or_create_by(request_type_id: params["request_type_id"]),
  #   event = Event.find_or_create_by(event_id: params["event_id"]),
  #   user_agent_info = UserAgentInfo.find_or_create_by(user_agent_info_id: params["user_agent_info_id"]),
  #   screen_size = ScreenSize.find_or_create_by(screen_size_id: params["screen_size_id"]),
  #   ip = Ip.find_or_create_by(ip_id: params["ip_id"]),
  #
  #   payload = PayloadRequest.save(url: url, requested_at: params["requested_at"],   responded_in: params["responded_in"], referred_by, request_type, parameters: params["parameters"], event, user_agent_info, screen_size, ip, payload_sha)

  # end

end
